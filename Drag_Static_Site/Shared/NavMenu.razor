<MudNavMenu>
    <MudText Typo="Typo.h6" Class="px-4">Drag_Static_Blog</MudText>
    <MudText Typo="Typo.body1" Class="px-4 mud-text-secondary">A Blazor WASM Blog</MudText>
    <MudDivider Class="my-2" />
    <MudNavLink Icon="@Icons.Material.Filled.Home" Href="/" >Index</MudNavLink>
</MudNavMenu>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
